import { Topic } from "../..";
import Demografie from "./Demografie";

const topics: Topic[] = [
    {
        name: 'Demografie',
        id: 'demografie',
        homepage: Demografie,
        description: 'Kolik morgošů je v čr'
    }
];

export default topics;
